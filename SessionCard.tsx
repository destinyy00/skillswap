import React from 'react';
import { Card, CardContent, CardHeader, Badge, Button } from '@shadcn/ui';
import { Calendar, Clock, MapPin, User } from 'lucide-react';
import { format } from 'date-fns';

// Rest of the component... 