// Example shared type definitions
export type User = {
  id: string;
  name: string;
  email: string;
  avatarUrl?: string;
};

export type Skill = {
  id: string;
  name: string;
  category: string;
  description: string;
}; 